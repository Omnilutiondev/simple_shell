#include "shell.h"

/**
 * main - This is the main entry point
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)

{
	/* This is a loop that makes the program continually display a prompt */
	while (1)
	{
	readmyCommand();/* This is the function for getting user input */
	}
	return (0);
}
